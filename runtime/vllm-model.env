MODEL="Qwen/Qwen2.5-Coder-14B-Instruct-AWQ"
PORT=18000
GPU_MEM="0.50"
MAX_MODEL_LEN="8192"
MAX_NUM_SEQS="1"
MAX_BATCHED_TOKENS="8192"
SWAP_SPACE_GB="4"
QUANTIZATION="awq_marlin"
ENFORCE_EAGER="true"
# Daily-driver stability profile for RTX 4090 under WSL2. Higher profiles
# monopolized VRAM and made Windows unusable. Keep a single 8k sequence by
# default; use an explicit high-context profile only for planned batch work.
# Tool calling support for Hermes / LiteLLM "auto" tool_choice (flags added to start-vllm-factory.sh):
# --enable-auto-tool-choice --tool-call-parser hermes
# Required because Hermes (and agents) use many MCP + built-in tools with auto selection.
# See Hermes chat error: "auto" tool choice requires --enable-auto-tool-choice and --tool-call-parser
